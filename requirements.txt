# Core runtime. This is everything the bot needs to run rules-only, which is
# the default mode - no LLM, no API key.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.7.0
PyYAML>=6.0
python-dotenv>=1.0.0
truststore>=0.9.0
slack-sdk>=3.27.0

# Optional: only needed for LLM_MODE=auto or always.
#   pip install -r requirements-llm.txt
